@model BilgiTekIzinTakip.Entities.Mudurluk

@{
    ViewBag.Title = "Müdürlük";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Sil</h2>

<h3>Bunu silmek istediğinizden emin misiniz?</h3>
<div>
    <h4>Müdürlük</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            <label class="col-md-3 m-t-15">Müdürlük İsmi</label>
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Isim)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

    <div class="form-actions no-color">
        @Bootstrap.Button("danger", "btn-secondary", "submit", "trash-alt", "Sil")

        @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-secondary" })
    </div>
    }
</div>
